# Team composition (Pokemon-Zone)

Derived tournament team-composition data (species co-occurrence / cores).
Not a bulk mirror of Pokemon-Zone. Not merged into the resolved-build cache.

| Path | Role |
|------|------|
| `champions-reg-mb.v1.json` | Reg M-B cores + pair counts from `/champions/team-cores/` (Limitless) |

Slug note: Pokemon-Zone uses `/champions/` + UI label `Regulation M-B` — distinct from Smogon `vgc-2026-regulation-m-b` and Pikalytics `battledataregmbs3`.
